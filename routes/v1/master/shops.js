// const router = require('express')()
// const { shops } = require('../../../controllers/index')
// const auth = require('../../../helpers/auth')

// router.get('/', auth.verifyToken, shops.getData)
// router.post('/', auth.verifyToken, shops.postData)
// router.put('/:shop_id', auth.verifyToken, shops.editData)
// router.delete('/:shop_id', auth.verifyToken, shops.deleteDataSoft)

// module.exports = router