// const router = require('express')()
// const { plant } = require('../../../controllers/index')
// const auth = require('../../../helpers/auth')

// router.get('/', auth.verifyToken, plant.getData)
// router.post('/', auth.verifyToken, plant.postData)
// router.put('/:plant_id', auth.verifyToken, plant.editData)
// router.delete('/:plant_id', auth.verifyToken, plant.deleteDataSoft)

// module.exports = router