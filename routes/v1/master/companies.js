// const router = require('express')()
// // const { company } = require('../../../controllers/index')
// const auth = require('../../../helpers/auth')

// router.get('/', auth.verifyToken, company.getData)
// router.post('/', auth.verifyToken, company.postData)
// router.put('/:company_id', auth.verifyToken, company.editData)
// router.delete('/:company_id', auth.verifyToken, company.deleteDataSoft)

// module.exports = router